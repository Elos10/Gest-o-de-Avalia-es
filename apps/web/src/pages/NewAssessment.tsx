import {useEffect,useMemo,useState} from 'react';import {useNavigate} from 'react-router-dom';import {allowedSubjects,questionCountFor,type Subject} from '@omr/core/rules';import {api} from '../lib/api';import type {Assessment,SchoolClass,Unit} from '../lib/types';import {Page} from '../components/Page';
export function NewAssessment(){const nav=useNavigate();const [classes,setClasses]=useState<SchoolClass[]>([]),[units,setUnits]=useState<Unit[]>([]),[unitId,setUnitId]=useState(''),[grade,setGrade]=useState(7),[subject,setSubject]=useState<Subject>('MATHEMATICS'),[message,setMessage]=useState('');useEffect(()=>{Promise.all([api<SchoolClass[]>('/api/classes'),api<Unit[]>('/api/units')]).then(([c,u])=>{setClasses(c);setUnits(u);if(u[0])setUnitId(u[0].id)}).catch(e=>setMessage(e.message))},[]);const availableClasses=classes.filter(x=>x.unitId===unitId&&x.grade===grade);const subjects=allowedSubjects(grade),safeSubject=subjects.includes(subject)?subject:subjects[0],count=useMemo(()=>questionCountFor(grade,safeSubject),[grade,safeSubject]);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const raw=Object.fromEntries(new FormData(e.currentTarget));try{const created=await api<Assessment>('/api/assessments',{method:'POST',body:JSON.stringify({...raw,unitId,grade,year:Number(raw.year),subject:safeSubject})});nav(`/avaliacoes/${created.id}`)}catch(e){setMessage((e as Error).message)}}return <Page eyebrow="Avaliações" title="Nova avaliação" description="A série define automaticamente disciplina permitida e quantidade de questões."><form className="card grid max-w-4xl gap-5 md:grid-cols-2" onSubmit={submit}><label><span className="label">Unidade</span><select className="field" value={unitId} onChange={e=>setUnitId(e.target.value)} required>{units.map(x=><option value={x.id} key={x.id}>{x.name}</option>)}</select></label><label><span className="label">Série</span><select className="field" value={grade} onChange={e=>{const g=Number(e.target.value);setGrade(g);setSubject(g===1?'SINGLE':'MATHEMATICS')}}>{Array.from({length:9},(_,i)=><option value={i+1} key={i}>{i+1}º ano</option>)}</select></label><label><span className="label">Turma</span><select className="field" name="classId" required><option value="">Selecione</option>{availableClasses.map(x=><option value={x.id} key={x.id}>{x.name} — {x.schoolYear}</option>)}</select></label><label><span className="label">Disciplina</span><select className="field" value={safeSubject} onChange={e=>setSubject(e.target.value as Subject)}>{subjects.map(x=><option value={x} key={x}>{x==='SINGLE'?'Prova Única':x==='PORTUGUESE'?'Língua Portuguesa':'Matemática'}</option>)}</select></label><label><span className="label">Número da avaliação</span><input className="field" name="number" defaultValue="001" required/></label><label><span className="label">Ano da avaliação</span><input className="field" type="number" name="year" defaultValue={new Date().getFullYear()} required/></label><label><span className="label">Data</span><input className="field" type="date" name="assessmentDate" required/></label><label><span className="label">Tempo</span><select className="field" name="timeMode"><option value="PARTIAL">Parcial</option><option value="FULL">Integral</option></select></label><div className="rounded-xl bg-mint p-4 md:col-span-2"><div className="text-sm text-forest">Quantidade definida pela regra central</div><div className="text-3xl font-bold">{count} questões</div></div><button className="btn md:col-span-2">Salvar e configurar gabarito</button>{message&&<p className="md:col-span-2 text-sm text-red-700">{message}</p>}</form></Page>}
