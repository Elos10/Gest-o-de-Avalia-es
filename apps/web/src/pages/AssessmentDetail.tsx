import {useEffect,useState} from 'react';import {useParams} from 'react-router-dom';import {Download,Printer,Users} from 'lucide-react';import {api,download} from '../lib/api';import type {Assessment} from '../lib/types';import {subjectLabel} from '../lib/types';import {Page} from '../components/Page';
const choices=['A','B','C','D','E'];
export function AssessmentDetail(){const {id}=useParams();const [item,setItem]=useState<Assessment>(),[key,setKey]=useState<string[]>([]),[message,setMessage]=useState('');const load=()=>api<Assessment>(`/api/assessments/${id}`).then(x=>{setItem(x);setKey(Array.from({length:x.questionCount},(_,i)=>x.key?.find(k=>k.question===i+1)?.choice??''))}).catch(e=>setMessage(e.message));useEffect(()=>{void load()},[id]);if(!item)return <p>{message||'Carregando…'}</p>;async function saveKey(){try{await api(`/api/assessments/${id}/answer-key`,{method:'PUT',body:JSON.stringify({choices:key})});setMessage('Gabarito oficial salvo.')}catch(e){setMessage((e as Error).message)}}async function generate(){try{await api(`/api/assessments/${id}/sheets`,{method:'POST',body:JSON.stringify({allStudents:true})});setMessage('Folhas geradas para todos os alunos.');load()}catch(e){setMessage((e as Error).message)}}return <Page eyebrow={`Avaliação ${item.number}`} title={`${subjectLabel(item.subject)} — ${item.grade}º ano`} description={`${item.unit.name} • ${item.schoolClass.name} • ${item.questionCount} questões`}><div className="grid gap-5 xl:grid-cols-[1.15fr_.85fr]"><section className="card"><div className="flex items-center justify-between"><h2 className="text-xl font-bold">Gabarito oficial</h2><button className="btn" onClick={saveKey}>Salvar respostas</button></div><div className="mt-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{key.map((value,i)=><label className="flex items-center gap-3 rounded-xl bg-paper p-3" key={i}><strong className="w-7">{String(i+1).padStart(2,'0')}</strong><select className="field !py-1.5" value={value} onChange={e=>setKey(k=>k.map((x,j)=>j===i?e.target.value:x))}><option value="">—</option>{choices.map(x=><option key={x}>{x}</option>)}</select></label>)}</div></section><aside className="space-y-5"><div className="card"><h2 className="text-xl font-bold">Folhas dos alunos</h2><p className="mt-2 text-sm text-black/50">Gere uma folha com QR exclusivo para cada estudante da turma.</p><button className="btn mt-4 flex w-full items-center justify-center gap-2" onClick={generate}><Users size={18}/> Gerar para a turma</button><div className="mt-4 divide-y">{item.sheets?.map(s=><div className="flex items-center justify-between py-3" key={s.id}><span className="text-sm font-semibold">{s.student?.name??'Folha sem aluno'}</span><button className="rounded-lg p-2 text-forest hover:bg-mint" onClick={()=>download(`/api/sheets/${s.id}/pdf`,`gabarito-${s.student?.name??s.id}.pdf`)}><Download size={18}/></button></div>)}</div></div><button className="card flex w-full items-center gap-3 text-left" onClick={()=>window.print()}><Printer className="text-forest"/><span><strong>Imprimir esta página</strong><small className="block text-black/45">Para as folhas use o PDF em tamanho real.</small></span></button>{message&&<div className="card text-sm">{message}</div>}</aside></div></Page>}

