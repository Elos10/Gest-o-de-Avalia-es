import {Camera,CheckCircle2,Upload} from 'lucide-react';import {useEffect,useState} from 'react';import {Link,useNavigate} from 'react-router-dom';import {api} from '../lib/api';import type {Processing} from '../lib/types';import {Page} from '../components/Page';
export function Reading(){const nav=useNavigate();const [file,setFile]=useState<File|null>(null),[items,setItems]=useState<Processing[]>([]),[message,setMessage]=useState('');useEffect(()=>{api<Processing[]>('/api/processings').then(setItems).catch(()=>{})},[]);async function process(){if(!file)return;setMessage('Processando imagem e QR…');const form=new FormData();form.append('file',file);try{const body=await api<Processing>('/api/processings',{method:'POST',body:form});nav(`/leitura/${body.id}/revisao`)}catch(e){setMessage((e as Error).message)}}return <Page eyebrow="Processamento" title="Leitura de gabaritos" description="Envie PDF, JPG ou PNG digitalizado. Página A4 inteira ou metade cortada."><div className="grid gap-5 xl:grid-cols-[.8fr_1.2fr]"><label className="card flex min-h-64 cursor-pointer flex-col items-center justify-center border-dashed text-center"><input className="hidden" type="file" accept="application/pdf,image/png,image/jpeg" onChange={e=>setFile(e.target.files?.[0]??null)}/>{file?<><CheckCircle2 className="text-forest" size={42}/><strong className="mt-3">{file.name}</strong><span className="text-sm text-black/45">{(file.size/1024/1024).toFixed(2)} MB</span></>:<><Upload className="text-forest" size={42}/><strong className="mt-3">Escolha uma digitalização</strong><span className="mt-1 text-sm text-black/45">PDF, PNG ou JPEG • até 15 MB</span></>}</label><div className="card"><div className="flex items-center gap-3"><Camera className="text-forest"/><h2 className="text-xl font-bold">Processamento automático</h2></div><ol className="mt-5 space-y-4 text-sm">{['Validar assinatura real do arquivo','Identificar QR e folha do aluno','Corrigir rotação e perspectiva','Medir preenchimento das bolhas','Separar leituras confiáveis e revisão'].map((x,i)=><li className="flex gap-3" key={x}><span className="grid h-6 w-6 shrink-0 place-items-center rounded-full bg-mint font-bold text-forest">{i+1}</span>{x}</li>)}</ol><button className="btn mt-7 w-full" disabled={!file} onClick={process}>Processar gabarito</button>{message&&<p className="mt-4 text-sm">{message}</p>}</div></div><h2 className="mt-9 text-xl font-bold">Processamentos recentes</h2><div className="card mt-3 overflow-x-auto"><table className="w-full text-left text-sm"><thead><tr className="border-b"><th className="pb-3">Aluno</th><th>Avaliação</th><th>Status</th><th>Data</th></tr></thead><tbody>{items.map(x=><tr className="border-b last:border-0" key={x.id}><td className="py-3 font-semibold"><Link to={`/leitura/${x.id}/revisao`}>{x.sheet?.student?.name??'Não identificado'}</Link></td><td>{x.sheet?.assessment.number??'—'}</td><td>{x.status}</td><td>{new Date(x.createdAt).toLocaleString('pt-BR')}</td></tr>)}</tbody></table></div></Page>}
