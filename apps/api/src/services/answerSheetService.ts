import {A4_LANDSCAPE_2UP_HORIZONTAL_V1 as template,generateAnswerSheetBatchPdf,generateAnswerSheetPdf,type SheetData} from '@omr/core';import type {Prisma} from '@prisma/client';import {db} from '../db.js';import {config,requiredSecret} from '../config.js';
export async function createSheet(assessmentId:string,studentId?:string){if(!studentId){const existing=await db.answerSheet.findFirst({where:{assessmentId,studentId:null}});return existing??db.answerSheet.create({data:{assessmentId,templateId:template.id,templateVersion:template.version}});}return db.answerSheet.upsert({where:{assessmentId_studentId:{assessmentId,studentId}},update:{templateId:template.id,templateVersion:template.version},create:{assessmentId,studentId,templateId:template.id,templateVersion:template.version}});}
const include={assessment:{include:{unit:true,schoolClass:true}},student:{include:{schoolClass:{include:{unit:true}}}}} as const;
type FullSheet=Prisma.AnswerSheetGetPayload<{include:typeof include}>;
function sheetData(sheet:FullSheet):SheetData{const a=sheet.assessment,studentClass=sheet.student?.schoolClass;return{sheetId:sheet.publicCode,assessmentId:a.id,assessmentNumber:a.number,assessmentYear:a.year,grade:a.grade,subject:a.subject,unitName:studentClass?.unit.name??a.unit.name,studentName:sheet.student?.name,className:a.schoolClass?.name??studentClass?.name??'Toda a rede',timeMode:a.timeMode,questionCount:a.questionCount};}
export async function sheetPdf(id:string){const sheet=await db.answerSheet.findUniqueOrThrow({where:{id},include});return generateAnswerSheetPdf(sheetData(sheet),requiredSecret('QR_HMAC_SECRET'),config.CALIBRI_FONT_PATH);}
export async function assessmentSheetsPdf(assessmentId:string){const sheets=await db.answerSheet.findMany({where:{assessmentId},include,orderBy:{createdAt:'asc'}}),collator=new Intl.Collator('pt-BR',{numeric:true,sensitivity:'base'});sheets.sort((a,b)=>collator.compare(a.student?.schoolClass.unit.name??a.assessment.unit.name,b.student?.schoolClass.unit.name??b.assessment.unit.name)||(a.student?.schoolClass.grade??a.assessment.grade)-(b.student?.schoolClass.grade??b.assessment.grade)||collator.compare(a.student?.schoolClass.name??a.assessment.schoolClass?.name??'',b.student?.schoolClass.name??b.assessment.schoolClass?.name??'')||collator.compare(a.student?.name??'',b.student?.name??''));return generateAnswerSheetBatchPdf(sheets.map(sheetData),requiredSecret('QR_HMAC_SECRET'),config.CALIBRI_FONT_PATH);}
