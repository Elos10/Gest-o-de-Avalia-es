import {A4_LANDSCAPE_2UP_HORIZONTAL_V1 as template,generateAnswerSheetPdf} from '@omr/core';import {db} from '../db.js';import {config,requiredSecret} from '../config.js';
export async function createSheet(assessmentId:string,studentId?:string){return db.answerSheet.create({data:{assessmentId,studentId,templateId:template.id,templateVersion:template.version}});}
export async function sheetPdf(id:string){const sheet=await db.answerSheet.findUniqueOrThrow({where:{id},include:{assessment:{include:{unit:true,schoolClass:true}},student:{include:{schoolClass:true}}}});const a=sheet.assessment;return generateAnswerSheetPdf({sheetId:sheet.publicCode,assessmentId:a.id,assessmentNumber:a.number,assessmentYear:a.year,grade:a.grade,subject:a.subject,unitName:a.unit.name,studentName:sheet.student?.name,className:a.schoolClass?.name??sheet.student?.schoolClass.name??'Toda a rede',timeMode:a.timeMode,questionCount:a.questionCount},requiredSecret('QR_HMAC_SECRET'),config.CALIBRI_FONT_PATH);}
